<UserControl x:Class="dotnet_music_player_app.Resources.View.PlayLists"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:dotnet_music_player_app.Resources.ViewModel"
             xmlns:uc="clr-namespace:dotnet_music_player_app.Resources.View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="622"
             Style="{StaticResource PageStyle}">

    <UserControl.DataContext>
        <vm:PlayLists/>
    </UserControl.DataContext>
    <Grid>

        <Border CornerRadius="0 20 20 0" Background="#010409"/>
        <StackPanel>
            <TextBlock Text="Playlists"
                   FontSize="30"
                   FontWeight="Bold"
                   HorizontalAlignment="Left"
                   Margin="20 20 0 0"
                   Foreground="#21d763"/>

            <TextBlock Text="Playlist 1"
                   FontSize="20"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Left"
                   Margin="20 20 0 0"
                   Opacity="0.5"
                   Foreground="#21d763"/>

            <TextBlock Text="Playlist 1"
                   FontSize="20"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Left"
                   Margin="20 20 0 0"
                   Opacity="0.5"
                   Foreground="#21d763"/>


        </StackPanel>


    </Grid>
</UserControl>